# Quick Test for Import Module Routes
# Run these in VS Code with REST Client extension or copy to Postman

### Test 1: Health Check (should work immediately)
GET http://localhost:3000/api/health

### Test 2: Test Import Routes - should return empty connections but validate auth
GET http://localhost:3000/api/import/connections
Authorization: Bearer test-token-123

### Test 3: Test starting import session (will fail due to missing Microsoft connection, but should validate input)
POST http://localhost:3000/api/import/start
Authorization: Bearer test-token-123
Content-Type: application/json

{
  "provider": "microsoft",
  "options": {
    "includeCompletedTasks": false
  }
}
